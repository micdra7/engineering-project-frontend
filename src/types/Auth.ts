export type TLoginValues = {
  email: string;
  password: string;
};

export type TRegisterValues = {
  email: string;
  firstName: string;
  lastName: string;
  password: string;
  workspaceName: string;
};
